package arrays;

import java.util.Scanner;
import java.util.ArrayList;

public class forEachClassActivity {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner input = new Scanner(System.in);
		/*
		 * System.out.println("How many students are in your class?"); int
		 * students = input.nextInt(); int [] age = new int [students]; int sum
		 * = 0; for(int x = 0; x < age.length; x ++){
		 * System.out.println("What is the age of your student?"); age[x] =
		 * input.nextInt(); } for (int num : age){ sum = sum+num; } int average
		 * = sum/students; System.out.println(average);
		 */
		/*
		 * System.out.println("How many employees do you have"); int workers =
		 * input.nextInt(); double max = 0.0; ArrayList<Double> wages = new
		 * ArrayList<Double>(); for(int x = 0; x<workers; x ++){
		 * System.out.println("How much is your worker paid?");
		 * wages.add(input.nextDouble()); } for(double num : wages){
		 * if(num>max){ max = num; } } System.out.println(max);
		 */
		/*
		 * ArrayList<Circle> circle = new ArrayList<Circle>(); for(int x = 0; x
		 * < 10; x ++){ System.out.println("What is the radii"); double radius =
		 * input.nextDouble(); circle.add(new Circle(input.nextDouble())); } for
		 * (Circle num : circle){ System.out.println(num.area()); }
		 */
		
	}

}
